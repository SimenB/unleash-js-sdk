# Supply-chain hardening: never run dependency lifecycle scripts
# (preinstall/install/postinstall) during yarn install.
# Note: publishing is unaffected — releases go through `npm publish`
# (see .github/workflows/release.yml), which reads .npmrc, not this file.
ignore-scripts true
